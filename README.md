## Quick start

1. Copy env template and fill keys:

   ```bash
   cp .env.example .env
   ```
2. Start everything

   ```bash
   make up    # or: docker compose up -d --build
   ```

3. Open LibreChat at http://localhost:3080 (or whatever port you mapped)